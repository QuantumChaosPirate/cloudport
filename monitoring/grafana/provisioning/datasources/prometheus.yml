apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus container address inside Docker network
    url: http://prometheus:9090
    isDefault: true
    editable: false
